{
  "name": "shush",
  "version": "0.2.7",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build-trie.ts && bun build hooks/pretooluse.ts --outdir=hooks --target=node --format=esm --minify",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "bash-parser": "^0.5.0",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.2.27",
    "@types/bun": "latest",
    "typescript": "^5.7"
  }
}
