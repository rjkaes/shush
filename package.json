{
  "name": "shush",
  "version": "1.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build-trie.ts && bun run scripts/build-flag-rules.ts && bun build hooks/pretooluse.ts hooks/posttooluse.ts --outdir=hooks --target=node --format=esm --minify",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "unbash": "^2.2.0"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.2.27",
    "@types/bun": "latest",
    "fast-check": "^4.6.0",
    "typescript": "^5.7",
    "z3-solver": "^4.16.0"
  }
}
