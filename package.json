{
  "name": "shush",
  "version": "0.3.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build-trie.ts && bun run scripts/build-flag-rules.ts && bun build hooks/pretooluse.ts --outdir=hooks --target=node --format=esm --minify",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "unbash": "^2.2.0"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.2.27",
    "@types/bun": "latest",
    "typescript": "^5.7"
  }
}
